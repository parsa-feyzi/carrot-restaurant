import { Metadata } from 'next';

export const metadata: Metadata = {
    title: "cart",
    description: "carrot cart",
};

function Cart() {
  return (
    <div>Cart</div>
  )
}

export default Cart